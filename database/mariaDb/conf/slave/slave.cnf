[mysqld]
general_log = 1
general_log_file = /var/lib/mysql/general.log

# Master Server Configuration
server-id = 2
log_bin=slave-bin
binlog-format=mixed
log_slave_updates=on

#bind-address    = *
#skip-networking|
